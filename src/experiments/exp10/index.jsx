import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 10
 * Modular implementation for Experiment 10.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment10({ onBack }) {
  return (
    <ExperimentTemplate expNumber={10} onBack={onBack} />
  );
}
