import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 9
 * Modular implementation for Experiment 9.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment9({ onBack, onOpenProfile }) {
  return (
    <ExperimentTemplate expNumber={9} onBack={onBack} onOpenProfile={onOpenProfile} />
  );
}
