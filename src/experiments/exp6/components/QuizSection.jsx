import React from 'react';
import { UnifiedQuizSection } from '../../../components/common';

export const QUIZ_QUESTIONS = [
  {
    id: 1,
    question: 'What is a dense embedding in the context of semantic search?',
    options: [
      'A compressed ZIP archive of document texts',
      'A fixed-length numerical vector of real continuous values representing text meaning',
      'A sparse bag-of-words index containing exact word counts',
      'A binary bit string encoding text ASCII characters',
    ],
    answer: 1,
    explanation: 'A dense embedding is a fixed-length vector of real continuous numbers generated by a neural network that encodes semantic meaning — most or all dimensions are non-zero.',
  },
  {
    id: 2,
    question: 'Which pretrained sentence-transformer model is used in Experiment 12?',
    options: [
      'text-embedding-ada-002',
      'BERT-base-uncased',
      'all-MiniLM-L6-v2',
      'Word2Vec-300',
    ],
    answer: 2,
    explanation: 'Experiment 12 uses all-MiniLM-L6-v2 from the Sentence Transformers library — a distilled, efficient transformer producing 384-dimensional embeddings.',
  },
  {
    id: 3,
    question: 'What is the output vector dimension produced by all-MiniLM-L6-v2?',
    options: ['128', '768', '512', '384'],
    answer: 3,
    explanation: 'all-MiniLM-L6-v2 produces dense sentence embeddings of exactly 384 dimensions — each sentence maps to a 384-component floating-point vector.',
  },
  {
    id: 4,
    question: 'Why must the exact same embedding model be used for both documents and queries?',
    options: [
      'To save memory during execution',
      'So that documents and queries are mapped into the identical vector space for comparison',
      'Because different models produce identical vector representations',
      'To speed up document tokenization',
    ],
    answer: 1,
    explanation: 'Vectors from different embedding models occupy entirely different coordinate spaces. Using the same model guarantees that query and document vectors are directly comparable.',
  },
  {
    id: 5,
    question: 'What does Cosine Similarity compute between a query vector and a document vector?',
    options: [
      'The Euclidean distance between vector endpoints',
      'The total number of identical character strings',
      'The cosine of the angle (directional alignment) between vectors',
      'The sum of all vector element values',
    ],
    answer: 2,
    explanation: 'Cosine similarity = cos(θ) = (A · B) / (‖A‖ · ‖B‖). It measures directional alignment in high-dimensional space, ranging from –1 to +1.',
  },
  {
    id: 6,
    question: 'What does Top-K retrieval specify in a semantic search engine?',
    options: [
      'Return documents with similarity score greater than K',
      'Return the K highest-ranked documents ordered by similarity score',
      'Return every K-th document in the indexed collection',
      'Return documents containing at least K unique words',
    ],
    answer: 1,
    explanation: 'Top-K retrieval selects and ranks the top K documents with the highest cosine similarity scores relative to the query vector.',
  },
  {
    id: 7,
    question: 'What is the primary advantage of L2-normalising embedding vectors before computing similarity?',
    options: [
      'The dot product of L2-normalised vectors directly equals Cosine Similarity',
      'It reduces vector dimension from 384 to 2',
      'It removes all negative values from vectors',
      'It speeds up document text loading from disk',
    ],
    answer: 0,
    explanation: 'When vectors are normalised to unit length (‖v‖ = 1), their dot product u · v equals cosine similarity directly, avoiding the expensive normalisation at query time.',
  },
  {
    id: 8,
    question: 'How does dense semantic search overcome the vocabulary mismatch problem?',
    options: [
      'By replacing all words in the dataset with synonyms before indexing',
      'By mapping semantically related concepts and paraphrases into nearby points in vector space',
      'By using exact string matching algorithms like Boyer-Moore',
      'By converting text into alphabetical sorted order',
    ],
    answer: 1,
    explanation: 'Dense embeddings capture conceptual meaning. The model was trained to place semantically similar text near each other in vector space — regardless of surface-level word differences.',
  },
  {
    id: 9,
    question: 'When are document dense embeddings typically computed in a search system?',
    options: [
      'Pre-computed upfront (offline) and stored for fast query-time matching',
      'Computed only after search results are sorted by relevance',
      'Generated randomly every time a user types a character',
      'Never computed — only query embeddings are produced',
    ],
    answer: 0,
    explanation: 'Document embeddings are computed offline during the indexing phase so that at query time, only a single query embedding needs to be computed before fast dot-product matching.',
  },
  {
    id: 10,
    question: 'Is the Top-K retrieval parameter (e.g. K=5) dependent on the 384 embedding dimensions?',
    options: [
      'No — Top-K is the count of retrieved search results; 384 is the vector size. They are independent.',
      'Yes — Top-K must always equal 384',
      'Yes — Top-K represents sub-dimensions of the 384-D vector',
      'No — Top-K is the vocabulary size of the model',
    ],
    answer: 0,
    explanation: 'Top-K determines how many search results are returned (e.g. K=5). This is completely independent of the 384 dimensions in the embedding vector.',
  },
];

export default function QuizSection(props) {
  return (
    <UnifiedQuizSection
      expNumber={6}
      expTitle="Dense Vector Search & HNSW Indexing"
      rawQuestions={QUIZ_QUESTIONS}
      {...props}
    />
  );
}
