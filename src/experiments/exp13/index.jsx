import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 13
 * Modular implementation for Experiment 13.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment13({ onBack, onOpenProfile }) {
  return (
    <ExperimentTemplate expNumber={13} onBack={onBack} onOpenProfile={onOpenProfile} />
  );
}
