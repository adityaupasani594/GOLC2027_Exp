import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 5
 * Modular implementation for Experiment 5.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment5({ onBack, onOpenProfile }) {
  return (
    <ExperimentTemplate expNumber={5} onBack={onBack} onOpenProfile={onOpenProfile} />
  );
}
