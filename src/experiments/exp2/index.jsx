import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 2
 * Modular implementation for Experiment 2.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment2({ onBack }) {
  return (
    <ExperimentTemplate expNumber={2} onBack={onBack} />
  );
}
