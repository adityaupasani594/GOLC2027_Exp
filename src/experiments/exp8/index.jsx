import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 8
 * Modular implementation for Experiment 8.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment8({ onBack, onOpenProfile }) {
  return (
    <ExperimentTemplate expNumber={8} onBack={onBack} onOpenProfile={onOpenProfile} />
  );
}
