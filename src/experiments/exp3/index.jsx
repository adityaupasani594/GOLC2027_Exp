import React from 'react';
import ExperimentTemplate from '../ExperimentTemplate';

/**
 * Experiment 3
 * Modular implementation for Experiment 3.
 * Customize or replace the tabs below with dedicated components.
 */
export default function Experiment3({ onBack }) {
  return (
    <ExperimentTemplate expNumber={3} onBack={onBack} />
  );
}
